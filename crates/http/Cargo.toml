[package]
name        = "http"
version     = "0.1.0"
edition     = "2021"
description = "writeonce wire protocol — native binary + GraphQL over WebSocket + REST dispatch"

[lib]
name = "http"
path = "src/lib.rs"
