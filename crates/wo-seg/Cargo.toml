[package]
name = "wo-seg"
version = "0.1.0"
edition = "2021"

[dependencies]
wo-model = { path = "../wo-model" }
serde = { workspace = true }
bincode = { workspace = true }
libc = { workspace = true }

[dev-dependencies]
tempfile = "3"
