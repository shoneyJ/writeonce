[package]
name        = "ui"
version     = "0.1.0"
edition     = "2021"
description = "writeonce UI renderer — `##ui` screens compile to SSR HTML + thin client runtime"

[lib]
name = "ui"
path = "src/lib.rs"
