-- Present only to make `secret` a real, discoverable module — this
-- fixture's whole point is that fixture.wo's own local variable named
-- `secret` must win over this module's alias, so nothing here is ever
-- meant to be reached.
fn hidden() -> Int {
  return -1
}
